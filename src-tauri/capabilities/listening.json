{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "listening",
  "description": "The dictation listening mark, which only exists on macOS",
  "platforms": ["macOS"],
  "windows": ["listening"],
  "permissions": ["core:default"]
}
